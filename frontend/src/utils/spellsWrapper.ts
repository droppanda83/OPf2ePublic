/**
 * Wrapper for spells to ensure proper module resolution
 */
export { SPELL_CATALOG, getSpell, getSpellByName, resolveSpellId, rollDamageFormula } from '../../../shared/spells';
export type { Spell, DamageType } from '../../../shared/spells';
